# SEO Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all high-priority SEO issues identified in the May 2026 audit — missing assets, thin titles, fake sitemap dates, missing structured data, and missing meta tags.

**Architecture:** Small, independent changes across existing Svelte components and static assets. One new component (`Breadcrumbs.svelte`) follows the existing `seo/` component pattern. All changes are backwards-compatible — no routing, layout, or data model changes.

**Tech Stack:** SvelteKit 2, Svelte 4, TypeScript, Tailwind CSS 4, Cloudflare Pages adapter. No test framework — verification via `yarn check` (TypeScript) and `yarn build` (SSG pre-render).

---

## File Structure

### New Files

| File | Responsibility |
|------|---------------|
| `src/lib/components/seo/Breadcrumbs.svelte` | Reusable BreadcrumbList JSON-LD component |
| `static/site.webmanifest` | Web app manifest for PWA/bookmarking metadata |

### Modified Files

| File | Changes |
|------|---------|
| `src/lib/components/seo/MetaTags.svelte` | Add `twitter:site` meta tag |
| `src/routes/about/+page.svelte` | Improve title + description, add breadcrumbs |
| `src/routes/products/+page.svelte` | Improve title + description, add breadcrumbs |
| `src/routes/contact/+page.svelte` | Improve title + description, add breadcrumbs |
| `src/routes/privacy/+page.svelte` | Add breadcrumbs |
| `src/routes/terms/+page.svelte` | Add breadcrumbs |
| `src/routes/cookies/+page.svelte` | Add breadcrumbs |
| `src/routes/+error.svelte` | Add `noindex` meta tag |
| `src/routes/sitemap.xml/+server.ts` | Replace dynamic `lastmod` with static dates |
| `src/app.html` | Add manifest and apple-touch-icon links |
| `docs/guidelines/seo-standards.md` | Document new Breadcrumbs component and twitter:site |

---

### Task 1: Add `twitter:site` to MetaTags Component

**Files:**
- Modify: `src/lib/components/seo/MetaTags.svelte:29-32`

- [ ] **Step 1: Add `twitter:site` meta tag**

In `src/lib/components/seo/MetaTags.svelte`, add one line after the existing Twitter card tags. Find:

```svelte
	<!-- TWITTER CARD -->
	<meta name="twitter:card" content="summary_large_image" />
	<meta name="twitter:title" content={title} />
	<meta name="twitter:description" content={description} />
	<meta name="twitter:image" content={image} />
</svelte:head>
```

Replace with:

```svelte
	<!-- TWITTER CARD -->
	<meta name="twitter:card" content="summary_large_image" />
	<meta name="twitter:site" content="@arbenger" />
	<meta name="twitter:title" content={title} />
	<meta name="twitter:description" content={description} />
	<meta name="twitter:image" content={image} />
</svelte:head>
```

- [ ] **Step 2: Verify TypeScript passes**

Run: `yarn check`
Expected: No errors related to MetaTags.svelte.

- [ ] **Step 3: Commit**

```bash
git add src/lib/components/seo/MetaTags.svelte
git commit -m "fix(seo): add twitter:site meta tag to MetaTags component"
```

---

### Task 2: Improve Title Tags and Meta Descriptions

**Files:**
- Modify: `src/routes/about/+page.svelte:13-17`
- Modify: `src/routes/products/+page.svelte:50-54`
- Modify: `src/routes/contact/+page.svelte:24-28`

Current titles are too short (16-19 chars) and waste ranking potential. Target: 40-60 chars with keywords.

- [ ] **Step 1: Update About page title and description**

In `src/routes/about/+page.svelte`, find:

```svelte
<MetaTags
	title="About | Arbenger"
	description="What Arbenger does and how we approach building software tools."
	url="https://arbenger.com/about"
/>
```

Replace with:

```svelte
<MetaTags
	title="About Arbenger — Who We Are & What We Build"
	description="Arbenger builds VS Code extensions, Chrome plugins, AI tools, and web apps. Learn about our approach to making software tools that stay out of your way."
	url="https://arbenger.com/about"
/>
```

- [ ] **Step 2: Update Products page title and description**

In `src/routes/products/+page.svelte`, find:

```svelte
<MetaTags
	title="Products | Arbenger"
	description="Browse Arbenger's catalog — VS Code extensions, Chrome plugins, AI tools, and web apps."
	url="https://arbenger.com/products"
/>
```

Replace with:

```svelte
<MetaTags
	title="Products — Extensions, Plugins & AI Tools | Arbenger"
	description="Browse Arbenger's full product catalog. VS Code extensions, Chrome plugins, AI tools, and web applications — all in one place. More coming soon."
	url="https://arbenger.com/products"
/>
```

- [ ] **Step 3: Update Contact page title and description**

In `src/routes/contact/+page.svelte`, find:

```svelte
<MetaTags
	title="Contact | Arbenger"
	description="Reach out to Arbenger. We'd like to hear from you."
	url="https://arbenger.com/contact"
/>
```

Replace with:

```svelte
<MetaTags
	title="Contact Arbenger — Questions, Ideas & Feedback"
	description="Get in touch with the Arbenger team. Have a question, feedback, or idea? Reach out via email or find us on GitHub, Twitter, and LinkedIn."
	url="https://arbenger.com/contact"
/>
```

- [ ] **Step 4: Verify TypeScript passes**

Run: `yarn check`
Expected: No errors.

- [ ] **Step 5: Commit**

```bash
git add src/routes/about/+page.svelte src/routes/products/+page.svelte src/routes/contact/+page.svelte
git commit -m "fix(seo): expand title tags and meta descriptions on inner pages"
```

---

### Task 3: Add `noindex` to Error Page

**Files:**
- Modify: `src/routes/+error.svelte:1-5`

Error pages should never appear in search results. Add a `noindex` directive.

- [ ] **Step 1: Add noindex meta tag**

In `src/routes/+error.svelte`, find:

```svelte
<script lang="ts">
	// IMPORTED DEP-MODULES
	import { page } from '$app/stores';

	// IMPORTED COMPONENTS
	import Button from '$lib/components/ui/Button.svelte';
</script>
```

Add after the closing `</script>` tag and before the `<!-- ERROR PAGE -->` comment:

```svelte
</script>

<svelte:head>
	<meta name="robots" content="noindex" />
	<title>{$page.status} | Arbenger</title>
</svelte:head>

<!-- ERROR PAGE -->
```

The full replacement — find:

```svelte
</script>

<!-- ERROR PAGE -->
```

Replace with:

```svelte
</script>

<svelte:head>
	<meta name="robots" content="noindex" />
	<title>{$page.status} | Arbenger</title>
</svelte:head>

<!-- ERROR PAGE -->
```

- [ ] **Step 2: Verify TypeScript passes**

Run: `yarn check`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/routes/+error.svelte
git commit -m "fix(seo): add noindex and title to error page"
```

---

### Task 4: Fix Sitemap `lastmod` Dates

**Files:**
- Modify: `src/routes/sitemap.xml/+server.ts`

The current implementation sets `lastmod` to today's date on every request, which makes the field meaningless. Google will learn to ignore it. Use static dates that reflect when content actually changed.

- [ ] **Step 1: Replace dynamic date with static dates**

Replace the entire contents of `src/routes/sitemap.xml/+server.ts` with:

```ts
import type { RequestHandler } from './$types';

const SITE_URL = 'https://arbenger.com';

const PAGES = [
	{ path: '/', priority: '1.0', changefreq: 'weekly', lastmod: '2026-05-09' },
	{ path: '/products', priority: '0.8', changefreq: 'weekly', lastmod: '2026-05-09' },
	{ path: '/about', priority: '0.6', changefreq: 'monthly', lastmod: '2026-05-09' },
	{ path: '/contact', priority: '0.5', changefreq: 'monthly', lastmod: '2026-05-09' },
	{ path: '/privacy', priority: '0.3', changefreq: 'yearly', lastmod: '2026-05-08' },
	{ path: '/terms', priority: '0.3', changefreq: 'yearly', lastmod: '2026-05-08' },
	{ path: '/cookies', priority: '0.3', changefreq: 'yearly', lastmod: '2026-05-08' },
];

export const GET: RequestHandler = async () => {
	const xml = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${PAGES.map(
	(page) => `  <url>
    <loc>${SITE_URL}${page.path}</loc>
    <lastmod>${page.lastmod}</lastmod>
    <changefreq>${page.changefreq}</changefreq>
    <priority>${page.priority}</priority>
  </url>`,
).join('\n')}
</urlset>`;

	return new Response(xml, {
		headers: {
			'Content-Type': 'application/xml',
			'Cache-Control': 'max-age=3600',
		},
	});
};

export const prerender = true;
```

Key change: each page now has a `lastmod` field with the actual date its content was last modified. When you change page content in the future, update the corresponding `lastmod` value here.

- [ ] **Step 2: Verify TypeScript passes**

Run: `yarn check`
Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add src/routes/sitemap.xml/+server.ts
git commit -m "fix(seo): use static lastmod dates in sitemap instead of dynamic today's date"
```

---

### Task 5: Create Breadcrumbs Component

**Files:**
- Create: `src/lib/components/seo/Breadcrumbs.svelte`

A reusable component that renders BreadcrumbList JSON-LD structured data. Used by all inner pages to help Google understand site hierarchy and display breadcrumb trails in search results.

- [ ] **Step 1: Create the Breadcrumbs component**

Create `src/lib/components/seo/Breadcrumbs.svelte`:

```svelte
<script lang="ts">
	// IMPORTED COMPONENTS
	import JsonLd from '$lib/components/seo/JsonLd.svelte';

	// -- REQUIRED PROPS -- //

	export let pageName: string;
	export let pageUrl: string;
</script>

<JsonLd
	schema={{
		'@context': 'https://schema.org',
		'@type': 'BreadcrumbList',
		itemListElement: [
			{
				'@type': 'ListItem',
				position: 1,
				name: 'Home',
				item: 'https://arbenger.com',
			},
			{
				'@type': 'ListItem',
				position: 2,
				name: pageName,
				item: pageUrl,
			},
		],
	}}
/>
```

- [ ] **Step 2: Verify TypeScript passes**

Run: `yarn check`
Expected: No errors. The `JsonLdSchema` type uses `[key: string]: unknown` so `itemListElement` is allowed.

- [ ] **Step 3: Commit**

```bash
git add src/lib/components/seo/Breadcrumbs.svelte
git commit -m "feat(seo): add reusable Breadcrumbs JSON-LD component"
```

---

### Task 6: Add Breadcrumbs to Main Pages

**Files:**
- Modify: `src/routes/about/+page.svelte:1-17`
- Modify: `src/routes/products/+page.svelte:1-54`
- Modify: `src/routes/contact/+page.svelte:1-28`

- [ ] **Step 1: Add breadcrumbs to About page**

In `src/routes/about/+page.svelte`, find the import block:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Button from '$lib/components/ui/Button.svelte';
```

Replace with:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Breadcrumbs from '$lib/components/seo/Breadcrumbs.svelte';
	import Button from '$lib/components/ui/Button.svelte';
```

Then, after the closing `/>` of the `<MetaTags ... />` tag, add:

```svelte
<Breadcrumbs pageName="About" pageUrl="https://arbenger.com/about" />
```

So the full block after `</script>` becomes:

```svelte
<MetaTags
	title="About Arbenger — Who We Are & What We Build"
	description="Arbenger builds VS Code extensions, Chrome plugins, AI tools, and web apps. Learn about our approach to making software tools that stay out of your way."
	url="https://arbenger.com/about"
/>

<Breadcrumbs pageName="About" pageUrl="https://arbenger.com/about" />
```

- [ ] **Step 2: Add breadcrumbs to Products page**

In `src/routes/products/+page.svelte`, find:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Badge from '$lib/components/ui/Badge.svelte';
```

Replace with:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Breadcrumbs from '$lib/components/seo/Breadcrumbs.svelte';
	import Badge from '$lib/components/ui/Badge.svelte';
```

Then, after the closing `/>` of the `<MetaTags ... />` tag, add:

```svelte
<Breadcrumbs pageName="Products" pageUrl="https://arbenger.com/products" />
```

- [ ] **Step 3: Add breadcrumbs to Contact page**

In `src/routes/contact/+page.svelte`, find:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
```

Replace with:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Breadcrumbs from '$lib/components/seo/Breadcrumbs.svelte';
```

Then, after the closing `/>` of the `<MetaTags ... />` tag, add:

```svelte
<Breadcrumbs pageName="Contact" pageUrl="https://arbenger.com/contact" />
```

- [ ] **Step 4: Verify TypeScript passes**

Run: `yarn check`
Expected: No errors.

- [ ] **Step 5: Commit**

```bash
git add src/routes/about/+page.svelte src/routes/products/+page.svelte src/routes/contact/+page.svelte
git commit -m "feat(seo): add BreadcrumbList JSON-LD to about, products, and contact pages"
```

---

### Task 7: Add Breadcrumbs to Legal Pages

**Files:**
- Modify: `src/routes/privacy/+page.svelte:1-13`
- Modify: `src/routes/terms/+page.svelte:1-12`
- Modify: `src/routes/cookies/+page.svelte:1-12`

- [ ] **Step 1: Add breadcrumbs to Privacy page**

In `src/routes/privacy/+page.svelte`, find:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
```

Replace with:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Breadcrumbs from '$lib/components/seo/Breadcrumbs.svelte';
```

Then, after the closing `/>` of the `<MetaTags ... />` tag and before `<!-- PAGE HERO -->`, add:

```svelte
<Breadcrumbs pageName="Privacy Policy" pageUrl="https://arbenger.com/privacy" />
```

- [ ] **Step 2: Add breadcrumbs to Terms page**

In `src/routes/terms/+page.svelte`, find:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
```

Replace with:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Breadcrumbs from '$lib/components/seo/Breadcrumbs.svelte';
```

Then, after the closing `/>` of the `<MetaTags ... />` tag and before `<!-- PAGE HERO -->`, add:

```svelte
<Breadcrumbs pageName="Terms of Service" pageUrl="https://arbenger.com/terms" />
```

- [ ] **Step 3: Add breadcrumbs to Cookies page**

In `src/routes/cookies/+page.svelte`, find:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
```

Replace with:

```svelte
	// IMPORTED COMPONENTS
	import MetaTags from '$lib/components/seo/MetaTags.svelte';
	import Breadcrumbs from '$lib/components/seo/Breadcrumbs.svelte';
```

Then, after the closing `/>` of the `<MetaTags ... />` tag and before `<!-- PAGE HERO -->`, add:

```svelte
<Breadcrumbs pageName="Cookie Policy" pageUrl="https://arbenger.com/cookies" />
```

- [ ] **Step 4: Verify TypeScript passes**

Run: `yarn check`
Expected: No errors.

- [ ] **Step 5: Commit**

```bash
git add src/routes/privacy/+page.svelte src/routes/terms/+page.svelte src/routes/cookies/+page.svelte
git commit -m "feat(seo): add BreadcrumbList JSON-LD to legal pages"
```

---

### Task 8: Create Web App Manifest and Update `app.html`

**Files:**
- Create: `static/site.webmanifest`
- Modify: `src/app.html:6-7`

The web manifest enables "Add to Home Screen" and provides metadata for browsers and search engines.

- [ ] **Step 1: Create `static/site.webmanifest`**

Create `static/site.webmanifest`:

```json
{
	"name": "Arbenger",
	"short_name": "Arbenger",
	"description": "Extensions, plugins, AI tools, and web apps.",
	"start_url": "/",
	"display": "browser",
	"background_color": "#F8FAFC",
	"theme_color": "#0891B2",
	"icons": [
		{
			"src": "/arbenger.svg",
			"type": "image/svg+xml",
			"sizes": "any"
		},
		{
			"src": "/arbenger.png",
			"type": "image/png",
			"sizes": "192x192"
		}
	]
}
```

- [ ] **Step 2: Add manifest and apple-touch-icon links to `app.html`**

In `src/app.html`, find:

```html
		<link rel="icon" href="%sveltekit.assets%/arbenger.svg" type="image/svg+xml" />
```

Replace with:

```html
		<link rel="icon" href="%sveltekit.assets%/arbenger.svg" type="image/svg+xml" />
		<link rel="apple-touch-icon" href="%sveltekit.assets%/arbenger.png" />
		<link rel="manifest" href="%sveltekit.assets%/site.webmanifest" />
```

- [ ] **Step 3: Verify build succeeds**

Run: `yarn build`
Expected: Build completes successfully with no errors.

- [ ] **Step 4: Commit**

```bash
git add static/site.webmanifest src/app.html
git commit -m "feat(seo): add web app manifest and apple-touch-icon link"
```

---

### Task 9: Asset Checklist (Manual — No Code)

These assets need to be created using a design tool. The codebase already references them but they don't exist in `static/` yet.

- [ ] **Step 1: Create OG image**

Create `static/og-image.png`:
- Dimensions: 1200 x 630 px
- Content: Arbenger logo + tagline on brand background
- Used by: `MetaTags.svelte` defaults to `https://arbenger.com/og-image.png` for all pages

Without this file, every social media share (Twitter, LinkedIn, Slack, Discord) shows a broken image.

- [ ] **Step 2: Verify `arbenger.png` dimensions**

Check that `static/arbenger.png` is at least 180x180 px (required for `apple-touch-icon`). If it's smaller, create a 180x180 version or a separate `apple-touch-icon.png` and update the link in `app.html`.

- [ ] **Step 3: Optional — Create `favicon.ico`**

Create `static/favicon.ico` containing 16x16 and 32x32 versions of the logo. Some crawlers and older browsers request `/favicon.ico` by convention. The existing SVG icon link handles modern browsers, so this is a nice-to-have.

If created, add this line to `src/app.html` before the SVG icon line:

```html
		<link rel="icon" href="%sveltekit.assets%/favicon.ico" sizes="32x32" />
```

---

### Task 10: Update SEO Standards Doc

**Files:**
- Modify: `docs/guidelines/seo-standards.md`

Document the new Breadcrumbs component and `twitter:site` tag so future pages follow the same pattern.

- [ ] **Step 1: Add Breadcrumbs section to the doc**

In `docs/guidelines/seo-standards.md`, find the section:

```markdown
### JsonLd Component Usage
```

Add a new section **before** it (after the Organization JSON-LD code block ends):

```markdown
### BreadcrumbList (All Inner Pages)

Every page except the homepage must include a BreadcrumbList using the `Breadcrumbs.svelte` component:

```svelte
<script lang="ts">
// IMPORTED COMPONENTS
import Breadcrumbs from '$lib/components/seo/Breadcrumbs.svelte';
</script>

<Breadcrumbs pageName="About" pageUrl="https://arbenger.com/about" />
```

This renders JSON-LD structured data that helps Google display breadcrumb trails in search results:

```json
{
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://arbenger.com" },
    { "@type": "ListItem", "position": 2, "name": "About", "item": "https://arbenger.com/about" }
  ]
}
```
```

- [ ] **Step 2: Add `twitter:site` to the Twitter Card section**

In `docs/guidelines/seo-standards.md`, find the Twitter Card Tags table:

```markdown
### Twitter Card Tags

| Tag | Requirement |
|-----|-------------|
| `twitter:card` | `summary_large_image` |
| `twitter:title` | Same as og:title |
| `twitter:description` | Same as og:description |
| `twitter:image` | Same as og:image |
```

Replace with:

```markdown
### Twitter Card Tags

| Tag | Requirement |
|-----|-------------|
| `twitter:card` | `summary_large_image` |
| `twitter:site` | `@arbenger` (hardcoded in MetaTags component) |
| `twitter:title` | Same as og:title |
| `twitter:description` | Same as og:description |
| `twitter:image` | Same as og:image |
```

- [ ] **Step 3: Add sitemap `lastmod` note**

In `docs/guidelines/seo-standards.md`, find:

```markdown
### Requirements

- Lists all indexable pages with absolute URLs
- Includes `<lastmod>` dates (ISO 8601 format)
- Updates automatically when pages are added
- Referenced in `robots.txt`
```

Replace with:

```markdown
### Requirements

- Lists all indexable pages with absolute URLs
- Includes static `<lastmod>` dates (ISO 8601 format) — update per-page when content changes
- New pages must be added to the `PAGES` array in `src/routes/sitemap.xml/+server.ts`
- Referenced in `robots.txt`
```

- [ ] **Step 4: Update the last-updated date**

In `docs/guidelines/seo-standards.md`, find:

```markdown
**Last updated:** 2026-05-09
```

No change needed if already `2026-05-09`. If a different date, update to `2026-05-09`.

- [ ] **Step 5: Commit**

```bash
git add docs/guidelines/seo-standards.md
git commit -m "docs: update SEO standards with Breadcrumbs component, twitter:site, and sitemap lastmod guidance"
```

---

### Task 11: Final Build Verification

- [ ] **Step 1: Run TypeScript check**

Run: `yarn check`
Expected: 0 errors, 0 warnings.

- [ ] **Step 2: Run production build**

Run: `yarn build`
Expected: All pages pre-render successfully. Output includes all routes: `/`, `/about`, `/products`, `/contact`, `/privacy`, `/terms`, `/cookies`, `/sitemap.xml`.

- [ ] **Step 3: Preview and spot-check**

Run: `yarn preview`

In a browser, check:
1. View source on homepage — verify `twitter:site` meta tag is present
2. View source on `/about` — verify updated title, description, and BreadcrumbList JSON-LD `<script>` tag
3. View source on `/products` — same checks
4. Navigate to a non-existent URL (e.g., `/nonexistent`) — verify `<meta name="robots" content="noindex">` is in the source
5. Visit `/sitemap.xml` — verify `<lastmod>` values are static dates, not today's date

No commit needed — this is verification only.
