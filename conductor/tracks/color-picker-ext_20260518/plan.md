# Implementation Plan: Color Picker Chrome Extension

**Track ID:** color-picker-ext_20260518
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-18
**Status:** [x] Complete

## Overview

Build a Manifest V3 Chrome extension with an eyedropper color picker, popup UI, and persistent color history. The extension directory lives at `extensions/color-picker/` in the monorepo. After the extension is functional, update arbenger.com product data to list it.

## Phase 1: Project Scaffolding

Set up the Chrome extension project structure, manifest, and build tooling.

### Tasks

- [x] Task 1.1: Create `extensions/color-picker/` directory structure (`src/`, `public/`, `dist/`)
- [x] Task 1.2: Create `manifest.json` (Manifest V3, permissions: `activeTab`, `storage`, `clipboardWrite`)
- [x] Task 1.3: Set up TypeScript config (`tsconfig.json`) and build script (esbuild + copy)
- [x] Task 1.4: Create extension icons (16, 32, 48, 128px) using Arbenger brand colors
- [x] Task 1.5: Create minimal popup HTML/CSS shell with Arbenger dark theme

### Verification

- [x] Extension builds to `dist/` without errors
- [x] Popup HTML, JS, manifest, icons all present in dist

## Phase 2: Eyedropper & Color Display

Implement the core eyedropper functionality and color format display.

### Tasks

- [x] Task 2.1: Implement eyedropper activation using `EyeDropper` API (Chrome 95+)
- [x] Task 2.2: Parse picked color into HEX, RGB, HSL formats
- [x] Task 2.3: Display all three formats in popup with color swatch preview
- [x] Task 2.4: Add copy-to-clipboard button per format (with visual feedback)
- [x] Task 2.5: Handle EyeDropper API unavailability with fallback message

### Verification

- [x] EyeDropper API called on button click, result parsed
- [x] All three formats display with correct conversion
- [x] Copy uses navigator.clipboard with execCommand fallback

## Phase 3: Color History

Add persistent history of picked colors using Chrome storage API.

### Tasks

- [x] Task 3.1: Implement color history storage using `chrome.storage.local`
- [x] Task 3.2: Build history panel UI (scrollable list, color swatches, format labels)
- [x] Task 3.3: Add click-to-copy on history items
- [x] Task 3.4: Add delete individual / clear all history actions
- [x] Task 3.5: Limit history to 50 recent colors, auto-prune oldest

### Verification

- [x] History persists via chrome.storage.local
- [x] Newest-first order, deduplication on re-pick
- [x] Delete and clear-all implemented

## Phase 4: Polish & Arbenger.com Integration

Finalize the extension UI and list it on the Arbenger website.

### Tasks

- [x] Task 4.1: Polish popup UI — hover states, transitions, responsive sizing
- [x] Task 4.2: Add "Powered by Arbenger" footer link in popup
- [x] Task 4.3: Update `src/lib/data/products.ts` — add color picker product entry
- [x] Task 4.4: Update `categories` productCount for `chrome-plugins` (0 → 1)
- [x] Task 4.5: Sitemap — no update needed (Chrome extension, not a web route)
- [x] Task 4.6: Write README.md for `extensions/color-picker/`

### Verification

- [x] Product appears in products.ts under Chrome Plugins category
- [x] svelte-check passes with 0 errors

## Final Verification

- [x] All acceptance criteria met
- [x] svelte-check: 0 errors, 0 warnings
- [x] Extension builds to dist/ with all required files
- [x] Product listing added to arbenger.com data
- [x] README documentation written

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
